import pytestfrom beopWeb.mod_admin import controllersimport jsonprojId=49@pytest.mark.p0@pytest.mark.parametrize(( 'data','expected'), [    ([projId],True),    (None,False),    ([],False),])def test_do_unbind_management(data,expected):    if expected:        controllers.do_bind_management(id,data)    rt=controllers.do_unbind_management(data)    rt = json.loads(bytes.decode(rt.data))    assert rt.get('success')==expected,' not equal .actual={0},expected={1}'.format(rt.get('data'),expected)