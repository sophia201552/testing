## 数据去重模块

data deduplication module

> 1. 容忍度: 去除连续重复的数据记录(通常预示着数据传输问题)

     tolerance: drop out continuous repeat records

## 数据补齐模块

data filling module

> 1. 前一列: 采用过去最近一个非缺失数据进行补齐

     previous column: filling missing data with the non-missing data which is close to the missing data

> 2. 均值: 采用当前数据列的均值进行补齐

     mean filling: filling missing data with the mean of the certain column

> 3. 插值: 采用前后路第一个非缺失数据的均值进行补齐

     interpolation: filling missing data with the average value of data which is previous and behind of the missing data

> 4. 最大补数间隔: 所允许的最大连续补齐个数

     maximum number to fill continuous missing data

## SVR 模块

svr module

> 1. cost(c): 代价函数

     cost(c): cost function

> 2. Epsilon: epsilon-SVR 模型中的差异容忍度

     Epsilon: the tolerance for stopping criterion aof SVR

> 3. Kernel: 核函数

     Kernel: kernel function

> 4. Linear: 线性函数

     Linear: linear function

> 5. Poly: 多项式函数

     Poly: polynomial function

> 6. RBF: 径向基函数

     RBF: radial basis function

> 7. Sigmoid: Sigmoid 函数

     Sigmoid: sigmoid function

> 8. Precomputed: 预先计算好的

     Precomputed: pre-calculated

> 9. CV Split Ratio: 用于交叉验证的测试集比例

     CV Split Ratio: the ratio of the dataset to include in the test split for cross-validation

## 归一化模块

Normalization module

> 1. 0-1: 使用归一化后的数据均值为 0, 方差为 1

     0-1: scaling data to zero mean and unit variance

> 2. Min max: 使用归一化的数据介于 0-1 之间

     Min max: scaling data to lie between 0 and 1

## 离群点检测

outlier detection

> 1. 离群点监测: 数值与其它数据相差较大

     outlier detection: the difference between the data and other data is large

> 2. 阈值: 离群点监测出阈值

     threshold value: points which are exceed the threshold value

> 3. 度量: 绝对值或百分比

     metrics: absolute value or percentage

> 4. 上下限: 正常数据范围

     upper and lower bound: the normal range

> 5. 污染率: 离群点比例

     contamination rate: the proportion of the outlier include in all dataset

> 6. 标记算法: 在决策函数上的离群点识别算法

     labeling algorithm: outlier detection based on decision function

## 模糊规则

fuzzy rule

> 1. 时序检查: 是否进行时序检查(0: 不检查 1: 检查)

     time sequential inspection: parameter for time sequential inspection, if it is 0, it should be checked, or otherwise.

> 2. 传感精度: 用在时序检查的波动, 是波动检查的判断参数

     accuracy of sensor: it is the parameter which is used for time sequential inspection

> 3. 类型: 点的类型, 比如: 4 公式三, 连续变量

     type: the type of points, such as continuous value,

> 4. 单位: 点的单位, 比如: 温度

     unit: the unit of points, such as temperature

> 5. 别名: 点的别名, 用于图标展示

     alias: the alias of a point, which is used for  visualization

> 6. term: 模糊定义, 将具体数值模糊化, 比如:将 20-28 °C 定位舒适

     term: the definition of fuzzy, which is used to fuzzy specific data, such as we define the indoor temperature is confortable when the temperature is between 20 and 28 °C

> 7. 模糊规则: 各个参数之间的逻辑关系, 比如: if today is Sunday the holiday is big

     fuzzy rule: the logical relationship between each parameter, such as if today is Sunday the holiday is big

## 测试评估

Test and Evaluation

> 1. AUC: 预测情况好坏的度量(>0.5 是说明优于随机猜测)

     auc: metrics for the diagnostic ability of a binary classifier

> 2. F1: F1 统计值

     F1: F1 score

> 3. CA:
> 4. precision: 准确率. 预测正确的比例

     precision: the accuracy of the positive predictions

> 5. recall: 召回率, 预测完整的比例

     recall: : the ratio of positive instances that are correctly detected by the classifier

> 6. 结果命中: 正确的结果

     target result: the right result

## 聚类

Clustering

> 1. DB-SCAN: 基于数据密度的聚类算法

     DB-SCAN: A clustering algorithm based on data density.

> 2. eps: 两个数据点被视为相邻的最大距离

     eps: The largest distance at which two data points can be seen as neighbors.

> 3. min_samples: 一个类中最少包含的数据点

     min_samples: The least number of data points in a cluster.

> 4. K-MEAN: k 均值聚类算法

     K-MEANS: The k-means clustering algorithm

> 5. n_clusters: 聚类的个数

     n_clusters: The number of clusters

## 数据诊断

Data Quality Diagnosis

> 1. 释义: 点的注释, 比如: chool-OnOff 1 号冷机开关

     Explanation: Explanation to a data point, etc. Ch001_OnOff is the on-off status of 1#chiller

> 2. DTU 名称: DTU 的注释, 比如: 717 hzhw-hnba BA 数据

     DTU Name: Annotation to a DTU, etc. 717 hzhw-hnba is the BA data.

> 3. 未变动监测: 是否需要监测数据未变动(0: 不监测 1: 监测)

     Constant Value Checking: Whether to check constant values (0: no 1: yes).

> 4. 未变动监测条件: 给足条件下再判断, 比如: Ch001_OnOff > 0 冷机开的时候

     Constant Value Checking Condition: Condition under which the constant value check takes effect, etc. Ch001_OnOff > 0 means checking only when 1#chiller is staged on.

> 5. 关联性表达式: 两个数据之间所有正确的关系显示, 例子: 见以前文档

     Relation Expression: Relations that two points should hold. Examples are shown in former documentation.

> 6. 掉线时间阈值: 掉线多久才算掉线

     Data Connection Failure Threshold: Tolerance for time of data connection failure.

> 7. 绝对突变阈值: 两个数据比较, 超过多少算变化太多

     Absolute Abrupt Change Threshold: The smallest distance between two data points that can be counted as abrupt change, measured in absolute value.

> 8. 相对突变阈值: 两个数据比较, 变化百分比超过多少算太多

     Relative Abrupt Change Threshold: The smallest distance between two data points that can be counted as abrupt change, measured in percentage.

> 9. 布尔数据波动监测: 监测 0, 1 变化的数据, 出现 0 到 1 次数是否过多

     Boolean Fluctuation Monitoring: Monitoring Boolean data to see whether it changes from 0 to 1 for too many times.

> 10. 报表展示分区标志: 一个数目向把不同类型的三分报表展示的标志

      Report Display Group ID: Sections sharing the same group ID will be put in the same report.

> 11. 交换机: 一个 DTU 会连多个交换机, 交换机会读很多点

      Exchanger: The subordinate devices to a DTU.

> 12. 累计量数据下降监测: 用电量数据下降检查（是否检查和下降多少报出）

      Cumulant Decreasing Monitoring: Whether to check the abnormal decreases in a cumulant, and how much decrease is seen as abnormal.

## PCA

PCA

> 1. PCA: 主成分分析

     PCA: Primary component analysis.

> 2. 维度: PCA 处理后剩下的数据维度(数据点个数)

     Number of Components: Number of components to keep.

> 3. 方差解释率: PCA 后的每个数据维度代表了原始数据的信息的比例

     Variance Explained Ratio: The amount of variance explained by each of the selected components

## Feature Selection

Feature Selection

> 1. 方差: 描述数据相对其均值离散程度的统计量

     variance: A scalar that measures how far a set of numbers is spread out

> 2. 方差阈值: 选取方差大于某一阈值的数据点

     Variance Threshold: The least variance a data series should have in order to be seleted.

> 3. 因变量: 被其他特征影响的数据点

     Dependent Variable: The variables that are influenced by others.

> 4. 评价函数: 评价自变量对因变量影响程度的方法

     Evaluating Function: Method for evaluating the influence a independent variable excerts on the dependent variables.

> 5. f 值分类: 对离散的固变量, 用 f 值衡量自变量对其影响程度

     f-classif: Use f-value to measure influence for discrete variables.

> 6. chi2: 用 chi-squared 分布衡量影响程度

     chi2: Use chi-squared distribution to measure influence for discrete or continuous variables.

> 7. f 值回归: 对连续的固变量, 用于 f 值衡量自变量对其影响程度

     f-regression: Using f-value to measure influence for continuous variables.

> 8. variance: 方差
> 9. KBEST: 选取 k 个对固变量影响最大的特征

     KBEST: Select the top k variables that have the most significant influence on the dependent variables.

> 10. PERCENTIEBEST: 按百分比选取出对固变量影响最大的特征

      PERCENTIEBEST: Select the top(in a percentage sense) variables that have the most significant influence on the dependent variables.
