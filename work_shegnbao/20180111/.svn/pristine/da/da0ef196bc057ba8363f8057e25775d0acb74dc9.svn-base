// 全局存储
window.appConfig = {
  project: {},
  user: {
    id: undefined
  },
  projectList: [
    
  ],
  language: localStorage.getItem('language') || 'zh',
  userProfile:{}
};
window.I18n = {
  type: localStorage.getItem('language') || 'zh'
};

export const API_HOST = '/strategyV2';
export const PATH_HOST = process.env.NODE_ENV === 'production' ? '/strategyV2' : '';