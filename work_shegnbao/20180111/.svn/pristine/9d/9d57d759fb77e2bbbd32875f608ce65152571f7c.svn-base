body,
html {
  height: 100%;
}
body {
  font-size: 12px;
  font-family: 'Helvetica', Arial, 'Microsoft YaHei', '微软雅黑';
  font-size: 12px;
  font-weight: 400;
  text-align: left;
  color: #333;
  position: relative;
}
body button,
body input,
body optgroup,
body select,
body textarea {
  font-family: 'Helvetica', Arial, 'Microsoft YaHei', '微软雅黑';
}
li {
  list-style: none;
}

div,
input,
h1,
h2,
h3,
h4,
span,
input,
button,
ul,
li,
table,
tbody,
thead,
tr,
th,
td {
  margin: 0;
  padding: 0;
  font-weight: 400;
  box-sizing: border-box;
}

/** iconfont 基础样式 - 不可删 */
.icon {
  width: 1em;
  height: 1em;
  vertical-align: -0.15em;
  fill: currentColor;
  overflow: hidden;
}
/** end */

.frame {
  width: 100%;
  height: 100%;
}
.frame .header {
  width: 100%;
  height: 0;
}
.frame .content {
  width: 100%;
  height: 100%;
}
/* 公共类样式 */
.clear-both:after {
  content: '';
  height: 0;
  line-height: 0;
  visibility: hidden;
  clear: both;
  display: block;
}

/* 模态框容器 */
.modal-container {
  width: 100%;
  height: 100%;
  border-radius: 6px;
  background-color: rgb(255, 255, 255);
  box-shadow: 0px 6px 20px 0 rgba(23, 28, 53, 0.5);
}

/* modalbutton 样式 */

.modal-button {
  width: 86px;
  height: 26px;
  margin: 8px 14px 0 0;
  border-radius: 4px;
  font-size: 12px;
  float: right;
  border: none;
}
.modal-button,
.modal-button:hover {
  background-color: #eda830;
  color: #fff;
}

.modal-button-close,
.modal-button-close:hover {
  background-color: transparent;
  border: 1px solid #eda830;
}

/* 导航条公共样式*/

.details-header {
  width: 100%;
  height: 40px;
  font-size: 10px;
  color: #6385c4;
  background-color: #2f3a54;
}
.details-header li:first-child {
  margin-left: 15px;
}
.details-header i {
  font-size: 13px;
  position: relative;
  top: 1px;
}

.details-header > ul li {
  height: 40px;
  line-height: 40px;
  float: left;
  display: flex;
  align-items: center;
  cursor: pointer;
}

.details-header > ul li > div {
  height: 100%;
  padding: 0 5px;
}
.details-header > ul li > div:hover {
  background-color: #292e48;
  color: #17a4df;
}
.details-line {
  padding: 0 5px;
  color: rgba(99, 133, 196, 0.2);
}

/* 高亮效果 */

.highLight {
  border: 2px solid red;
}

/* 进度条label */
.ms-Slider label {
  text-align: center;
}
/* 进度条label end*/

/* SVM 下拉框 start */
.ms-Layer--fixed .ms-Dropdown-callout {
  border: none;
}

.ms-Layer--fixed .ms-Dropdown-items .ms-Dropdown-item {
  min-height: auto;
  padding: 0 16px;
}
/* SVM 下拉框 end */

/* 滚动条样式*/
*::-webkit-scrollbar-track {
  -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
  background-color: #f5f5f5;
}

*::-webkit-scrollbar {
  width: 8px;
  height: 8px;
  background-color: #f5f5f5;
}

*::-webkit-scrollbar-thumb {
  background-color: rgba(0, 0, 0, 0.3);
}

/* 滚动条样式end */

/* 时间选择器 */
.react-datepicker__time-container
  .react-datepicker__time
  .react-datepicker__time-box
  ul.react-datepicker__time-list {
  padding-right: 0;
}
/* 时间选择器end */

/* modal */
.ms-Modal .ms-Overlay.ms-Overlay--dark{
  background-color: rgba(0,0,0,.6);
}
.ms-Modal .ms-Dialog-main{
  border-radius: 4px;
}
/* modal end */