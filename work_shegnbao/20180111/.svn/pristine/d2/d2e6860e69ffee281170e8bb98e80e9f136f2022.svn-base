import React from 'react';
import PropTypes from 'prop-types';
import { Toggle } from 'office-ui-fabric-react/lib/Toggle';

import s from './TextToggle.css';

export default function(props) {
  return (
    <Toggle
      {...props}
      styles={{
        text: s['toggle-text'],
        root: s['toggle-root'],
        pill: s['toggle-pill'],
        thumb: s['toggle-thumb']
      }}
    />
  );
}
