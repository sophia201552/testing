(function () {

    function PagePanel(screen) {
        this.screen = screen;
        this.container = screen.pagePanelCtn;

        this.init();
    }

    PagePanel.prototype.init = function () {
        
    };

    PagePanel.prototype.show = function () {
        
    };

    PagePanel.prototype.remove = function () {

    };

    window.PagePanel = PagePanel;

} ());